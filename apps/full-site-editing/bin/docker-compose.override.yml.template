services:
  wordpress-develop:
    &fse-base-service
    volumes:
      - ../full-site-editing-plugin:/var/www/${LOCAL_DIR-src}/wp-content/plugins/%PLUGIN_INSTALL_DIR%
  php:
    <<: *fse-base-servic
  cli:
    <<: *fse-base-service
  phpunit:
    <<: *fse-base-service
